export const manifest={version:1,commands:['doctor','deploy','up','funnel','serve','dns','policy','status','cleanup','agent-manifest'],rules:{first:'doctor --detect-credentials --json',secretHandling:'mask and never persist',policy:'minimal diff, backup, validate, ETag and confirmation',cleanup:'offline > 60m, hostname/tag match, confirmation',binary:'use existing; update only with --update-bin'}} as const;
