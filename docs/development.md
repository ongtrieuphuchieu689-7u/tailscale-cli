# Development

Requires Node 22 or 24. Run `npm install`, `npm run typecheck`, `npm test`, `npm run smoke`, and `npm run pack:check`. TypeScript is strict ESM NodeNext. Keep command output JSON-safe and update docs whenever flags or envelopes change.
